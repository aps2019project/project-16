package newView;

import javafx.scene.Parent;
import javafx.scene.Scene;

import static newView.SceneMaker.*;

public class MyScene extends Scene {
    public MyScene(Parent root) {
        super(root, WIDTH, HEIGHT);
    }
}
